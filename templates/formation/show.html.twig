{% extends 'base.html.twig' %}

{% block contenu %}
	<a type="button" href="{{ path('formation')}}" class="btn" style="padding-left: 0;">
		<i class="fas fa-arrow-left" style="font-size: 3rem"></i>
	</a>
	{# <p>Nom : {{ formation.nom ~ ' ' ~ formation.prenom }}</p>
		<p>Civilité : {{ formation.civilite }}</p>
		<p>Adresse mail : <a href="mailto:{{ formation.email }}">{{ formation.email }}</a></p>
		<p>Téléphone : {{ formation.telephone }}</p>
		<p>Date de naissance : {{ formation.dateNaissance | date ('d/m/Y') }}</p>
		<p>Ville : {{ formation.ville }}</p>
		<p>Adresse : {{ formation.adresse }}</p>
		<p>Code Postal : {{ formation.cp }}</p> #}
	<table>
		<tr>
			<th>Nom :</th>
			<td>{{ formation.nom }}</td>
		</tr>
		<tr>
			<th>Description :</th>
			<td>{{ formation.description }}</td>
		</tr>
	</table>
	{# <a type="button" class="btn btn-warning" href="{{ path('app_changepassword', {'id':app.user.id}) }}">Modifier MDP</a> #}
{% endblock %}
