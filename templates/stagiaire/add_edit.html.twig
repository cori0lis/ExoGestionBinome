{% extends 'base.html.twig' %}

{% set text = (editMode) ? 'Modifier':'Ajouter' %}

{% block title %}
Un Stagiaire
{% endblock %}

{% block contenu %}
<a type="button" href="{{ path('stagiaire')}}" class="btn" style="padding-left: 0;">
		<i class="fas fa-arrow-left" style="font-size: 3rem"></i>
	</a>

  <h1>{{ text }} salarié <span>{{ stagiaire }}</span></h1>

    {{ form(formStagiaire) }}

{% endblock %}