{% extends 'base.html.twig' %}

{% block contenu %}
<a type="button" class="btn btn-success ajouter" href="

{# {{ path('stagiaire_add')}} #}

">Ajouter</a>


    <table class="table">
        <tr>
            <th>NOM</th>
            <th>PRENOM</th>
            <th>CIVILITE</th>
            <th>EMAIL</th>
            <th>TELEPHONE</th>
            <th>DATE DE NAISSANCE</th>
            <th>VILLE</th>
            <th>ADRESSE</th>
            <th>CP</th>
            <th>Modifier</th>
            <th>Supprimer</th>
        </tr>
    </thead>
    <tbody>
        {% for stagiaire in stagiaires %}
            <tr>
                {# <td><a href="{{ path('stagiaire_show', {'id':stagiaire.id}) }}"> {{ stagiaire.nom ~ ' ' ~ stagiaire.prenom }}</a></td> #}
                <td>{{ stagiaire.nom }}</td>
                <td>{{ stagiaire.prenom }}</td>
                <td>{{ stagiaire.civilite }}</td>
                <td>{{ stagiaire.email }}</td>
                <td>{{ stagiaire.telephone }}</td>
                <td>{{ stagiaire.dateNaissance | date ('d/m/Y') }}</td>
                <td>{{ stagiaire.ville }}</td>
                <td>{{ stagiaire.adresse }}</td>
                <td>{{ stagiaire.cp }}</td>
                {# <td><a href="{{ path('entreprise_show', {'id':stagiaire.entreprise.id}) }}"> {{ stagiaire.entreprise.raisonsociale }}</a></td> #}
                {# <td><a type="button" class="btn btn-warning" href="{{ path('stagiaire_edit', {'id':stagiaire.id}) }}">Modifier</a></td> #}
                {# <td><a type="button" class="btn btn-danger" href="{{ path('stagiaire_delete', {'id':stagiaire.id}) }}">Supprimer</a></td> #}
            </tr>
        {% endfor %}
    </tbody>
</table>
{% endblock %}
