{% extends 'base.html.twig' %}

{% set text = (editMode) ? 'Modifier':'Ajouter' %}

{% block title %}
Un Employé
{% endblock %}

{% block contenu %}
<a type="button" href="{{ path('formation')}}" class="btn" style="padding-left: 0;">
		<i class="fas fa-arrow-left" style="font-size: 3rem"></i>
	</a>

  <h1>{{ text }} employé <span>{{ utilisateur }}</span></h1>

    {{ form(formAnnuaire) }}

{% endblock %}